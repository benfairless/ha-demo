#!/usr/bin/env bash

VENV='/vagrant/tmp/virtualenv'
source ${VENV}/bin/activate

export BACKEND
export PORT

BACKEND='frontend-01'
PORT='5000'

gunicorn --config '/vagrant/application/gunicorn.py' server:app
