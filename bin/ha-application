#!/usr/bin/env bash

VENV='/home/vagrant/ha-demo/venv'
source ${VENV}/bin/activate

export BACKEND
export PORT

BACKEND='frontend-01'
PORT='5000'

gunicorn --config '/vagrant/application/gunicorn.py' server:app
